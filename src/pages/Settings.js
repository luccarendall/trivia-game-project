import React, { Component } from 'react';

class Settings extends Component {
  render() {
    return (<section data-testid="settings-title"><h1>Settings</h1></section>);
  }
}

export default Settings;
